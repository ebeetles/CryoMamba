# Core FastAPI server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.31.0

# Core scientific computing
numpy>=1.23
torch>=2.1.0
psutil>=5.9.0

# Medical imaging formats
nibabel>=5.2.0
mrcfile>=1.4.0

# nnU-Net v2 dependencies (prevents installation issues)
# Note: If nnunetv2 installation fails, see troubleshooting section
# Alternative: Install from source: git clone https://github.com/MIC-DKFZ/nnUNet.git && cd nnUNet && pip install -e .
scikit-build>=0.17.0
batchgeneratorsv2>=0.3.0  # nnU-Net v2 requires batchgeneratorsv2 (not batchgenerators)
# nnunetv2>=2.0.0  # Commented out - install manually if needed

# Additional dependencies used in codebase
pydantic>=2.0.0
websockets>=10.0
aiohttp>=3.8.0
Pillow>=8.0.0
matplotlib>=3.5.0
